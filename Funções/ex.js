// Crie uma função para verificar se um valor é Truthy 
function isVerdadeiro (dado) {
  return !!dado;
}

// Crie uma função matemática que retorne o perímetro de um quadr // lembrando: perímetro é a soma dos quatro lados do quadrado 
function perimetroQuadrado (quadrado, lado) {
  var lado = 4;
  var perimetroQuadrado = quadrado * lado; 
  return perimetroQuadrado;
}

// Teste github //
perimetroQuadrado(20);


// Crie uma função que retorne o seu nome completo // ela deve possuir os parâmetros: nome e sobrenome 
function nomeCompleto (nome, sobrenome) {
  return nome + ' ' + sobrenome;
}

nomeCompleto();

// Crie uma função que verifica se um número é par 
function isPar (numero) {
  return numero / 2;
}
 
// Crie uma função que retorne o tipo de // dado do argumento passado nela (typeof) 
function tipoDeDado(dado) {
  return typeof dado;
}
 

